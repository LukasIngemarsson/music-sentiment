# Used by local package commands (`python -m music_sentiment.dry_run`,
# `python -m music_sentiment.weekly_post`).
LASTFM_API_KEY=

# For local webhook tests only. In GitHub Actions, set as a repository secret.
DISCORD_WEBHOOK_URL=

# Optional local testing flags for `python -m music_sentiment.weekly_post`.
DRY_RUN=true
